{
  "name": "zukyc",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "localenv": "turbo run localenv",
    "localdb": "docker-compose up"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.0.5"
  },
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=18"
  }
}
